---
# 前端架构方案摘要（供下游 skill / agent 快速读取）
framework:          # React / Vue / Astro
register:           # Content / Tool / Desktop / Hybrid
complexity_level:   # 1 / 2 / 3
decisive_dimension: # 团队规模 / 生命周期 / 业务复杂度
component_library:  # 主组件库名称
styling:            # Tailwind / CSS Modules / UnoCSS / Sass
state_management:   # 方案名称 / "未使用全局状态"
architecture_focus: # 一句话架构侧重
mvp_scope:          # 一句话 MVP 范围
created:            # YYYY-MM-DD
updated:            # YYYY-MM-DD
---

<!-- 下游 coding agent 使用指引：
     1. 先读 YAML frontmatter 了解全局决策
     2. 再读"架构约束"区了解不可违反的底线
     3. 按章节顺序理解架构方案
     4. 编码前检查 MVP 边界
-->

# [项目名称] 前端架构设计方案

## 1. 项目理解摘要

**项目名称**：
**项目类型**：官网 / 知识库 / 后台管理 / SaaS / 桌面应用 / 数据看板 / 移动端 / 其他
**核心用户**：
**核心场景**：
**前端形态**：展示型 / 工具型 / 管理型 / 编辑型 / 混合型
**技术约束**：有 / 无（列出已有约束）

---

## 2. 复杂度判定

| 维度 | 判定 | 说明 |
|---|---|---|
| 团队规模 | Level [X] | |
| 生命周期 | Level [X] | |
| 业务复杂度 | Level [X] | |

**最终 Level**：[X]（决定性维度：[维度]，架构侧重：[方向]）

---

## 3. 推荐技术栈

| 维度 | 选择 | 理由 |
|---|---|---|
| 框架 | | |
| 语言 | | |
| 构建工具 | | |
| 样式方案 | | |
| 组件库 | | |
| 状态管理 | | |
| 数据获取 | | |
| 图标库 | | |
| 路由 | | |

---

## 4. UI 组件库选择与理由

### 选择：[组件库名称]

### 选择理由

- **与 Register 匹配度**：
- **AI 友好度**：
- **生态成熟度**：
- **定制能力**：
- **Bundle 影响**：

### 排除方案（Level 2+）

| 被排除的库 | 排除理由 |
|---|---|
| | |

---

<!-- ====== 存量项目追加区（仅存量项目 / 重构场景填写，新项目删除本区）====== -->

## 当前结构诊断

### 现有技术栈

| 维度 | 当前方案 |
|---|---|
| 框架 | |
| 组件库 | |
| 样式方案 | |
| 状态管理 | |
| 构建工具 | |

### 问题分类

| 类别 | 问题 | 严重度 |
|---|---|---|
| 高风险 | | 🔴 |
| 低风险 | | 🟡 |
| 优势保留 | | ✅ |

### 保留项

| 保留内容 | 保留理由 |
|---|---|
| | |

### 调整项

| 调整内容 | 预期收益 | 成本 |
|---|---|---|
| | | |

### 迁移步骤

| 步骤 | 操作 | 验证方式 | 风险 | 回滚方案 |
|---|---|---|---|---|
| 1 | | | | |

<!-- ====== 存量项目追加区结束 ====== -->

---

## 5. 目录结构方案

```
project/
├── src/
│   ├── ...
├── public/
├── docs/
│   └── frontend-architecture.md
├── package.json
└── [配置文件]
```

### 目录职责说明

| 目录 | 职责 | 放什么 | 不放什么 |
|---|---|---|---|
| | | | |

---

## 6. 模块边界与领域拆分

<!-- Level 1：不输出（保留标题并标注"本项目复杂度较低，此项暂不展开"）-->
<!-- Level 2：简，给方向不展开 -->
<!-- Level 3：展开 -->

### 领域划分

| 域类型 | 模块 | 职责 | 依赖 |
|---|---|---|---|
| 核心域 | | | |
| 支撑域 | | | |
| 通用域 | | | |

### 模块间依赖规则

```
核心域 → 支撑域 → 通用域
（依赖方向：从左到右）
```

---

## 7. 核心组件清单

### 应用壳组件

| 组件 | 职责 | 优先级 |
|---|---|---|
| | | |

### 业务组件

| 组件 | 职责 | 所属模块 | 优先级 |
|---|---|---|---|
| | | | |

### 通用组件

| 组件 | 职责 | 优先级 |
|---|---|---|
| | | |

### 组件封装原则

1. 先封装稳定重复的结构
2. 不为了"看起来高级"而提前抽象
3. 业务组件和纯 UI 组件分开
4. 组件命名遵循 AI 友好规则：可搜索、语义明确、遵循约定、模式稳定

---

## 8. Design Token 初稿

<!-- Level 1：不输出 -->
<!-- Level 2+：输出 -->

### Spacing

```css
:root {
  --space-1: 0.25rem;   /* 4px */
  --space-2: 0.5rem;    /* 8px */
  --space-3: 0.75rem;   /* 12px */
  --space-4: 1rem;      /* 16px */
  --space-6: 1.5rem;    /* 24px */
  --space-8: 2rem;      /* 32px */
  --space-12: 3rem;     /* 48px */
  --space-16: 4rem;     /* 64px */
}
```

### Radius

```css
:root {
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;
  --radius-xl: 12px;
  --radius-full: 9999px;
}
```

### Color

```css
:root {
  /* 背景 */
  --color-bg: ;
  --color-bg-elevated: ;
  --color-bg-muted: ;

  /* 文字 */
  --color-text-primary: ;
  --color-text-secondary: ;
  --color-text-muted: ;

  /* 功能色 */
  --color-primary: ;
  --color-danger: ;
  --color-success: ;
  --color-warning: ;

  /* 边框 */
  --color-border: ;
  --color-border-strong: ;
}
```

### Typography

```css
:root {
  --font-sans: ;
  --font-mono: ;

  --text-xs: 0.75rem;    /* 12px */
  --text-sm: 0.875rem;   /* 14px */
  --text-base: 1rem;     /* 16px */
  --text-lg: 1.125rem;   /* 18px */
  --text-xl: 1.25rem;    /* 20px */
  --text-2xl: 1.5rem;    /* 24px */
  --text-3xl: 1.875rem;  /* 30px */

  --leading-normal: 1.5;
  --leading-relaxed: 1.75;
  --leading-tight: 1.25;
}
```

### Shadow

```css
:root {
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.07);
  --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
}
```

### Z-Index

```css
:root {
  --z-base: 0;
  --z-dropdown: 100;
  --z-sticky: 200;
  --z-overlay: 300;
  --z-modal: 400;
  --z-toast: 500;
  --z-tooltip: 600;
}
```

### Layout

```css
:root {
  --sidebar-width: 240px;
  --sidebar-collapsed-width: 64px;
  --topbar-height: 56px;
  --content-max-width: 1280px;
}
```

---

## 9. 数据与状态架构

<!-- Level 1：不输出 -->
<!-- Level 2+：输出 -->

### 状态分类

| 数据类型 | 方案 | 说明 |
|---|---|---|
| 服务端数据（server-state） | | |
| 全局 UI 状态（client-state） | | |
| 组件局部状态 | | |

### 数据获取层

- 请求封装入口位置：
- 错误处理统一策略：
- 缓存与失效策略：

### 全局状态边界

| 放全局 | 不放全局 |
|---|---|
| | |

---

## 10. 主题与国际化预留

<!-- Level 1/2：不输出 -->
<!-- Level 3：输出 -->

### 主题

| 能力 | MVP 阶段 | 后续 |
|---|---|---|
| Light / Dark / System | | |
| CSS 变量切换 | | |
| 用户偏好持久化 | | |

### 国际化

| 能力 | MVP 阶段 | 后续 |
|---|---|---|
| i18n 框架选择 | | |
| 语言包目录结构 | | |
| 文案 key 命名规则 | | |
| 日期 / 数字格式化 | | |

---

## 11. MVP 第一阶段范围

### 必须完成的页面

| 页面 | 核心功能 | 优先级 |
|---|---|---|
| | | P0 |

### 必须封装的组件

| 组件 | 理由 | 优先级 |
|---|---|---|
| | | P0 |

### 明确延后的能力

| 能力 | 延后理由 | 预计引入阶段 |
|---|---|---|
| | | |

### 可接受的技术债

| 技术债 | 接受理由 | 清理时机 |
|---|---|---|
| | | |

### 不可接受的技术债

-

---

## 12. 后续编码建议

### 编码前

1. 按本方案的目录结构创建项目骨架
2. 配置组件库、样式方案、状态管理
3. 实现 AppShell 和路由框架
4. 建立请求层和错误处理

### 编码中

1. 每个功能开发前检查是否在 MVP 范围内
2. 新增组件遵循分层原则（UI 组件 / 业务组件 / 页面组件）
3. 样式使用 Token，不硬编码颜色和间距
4. 命名遵循 AI 友好规则

### 编码后

1. 运行 decision-checklist 自检
2. 架构变更记录在下方"架构变更记录"区
3. 重大结构变更时重新评审复杂度

---

## 架构决策记录

<!-- Level 2+ 追加 -->

| 日期 | 决策 | 理由 | 替代方案 | 触发方 |
|---|---|---|---|---|
| | | | | |

---

## 架构约束（下游 skill 应遵守）

- **技术栈**：不得更改已选定的框架、构建工具、样式方案
- **目录结构**：新增目录应遵循已有的分层规则
- **组件库**：不得引入与已选组件库冲突的替代方案
- **状态管理**：不得绕过已定义的数据流架构
- **命名规范**：应遵循已定义的 AI 友好命名规则
- **MVP 边界**：超出 MVP 范围的功能应先更新本文件

---

## 架构变更记录

| 日期 | 变更内容 | 原因 | 触发方 |
|---|---|---|---|
| | | | |

<!-- 本模板基于 frontend-architecture-designer/core/output-template.md（2026-06-12 版）生成 -->
